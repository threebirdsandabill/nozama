

{{#if items}}

    {{#each items as |item|}}

        <div data-cardDivID="{{itemId}}">
          <div class="row">
            <div class="col-xs-4">
              <img src="https://s3.amazonaws.com/anna0312wdi/nozama-images/{{itemId.image}}" class="item-image-cart">
            </div>
            <div class="col-xs-8">
              <strong>{{itemId.name}}</strong>
              <br /> ${{itemId.price}}
              <br>
              <div class="form-inline">
                <div class="form-group">
                  <input type="number" min="1" class="form-control small-input" id="updateqty_{{itemId._id}}" name="item[qty]" data-qtyitemid="{{itemId._id}}" value="{{itemQty}}">
                </div>
                  <button id="btnupdateqty_{{itemId._id}}" type="submit" class="btn btn-info btn-update-item" data-btnupdateitemid="{{item._id}}">Update qty</button>

                  <button id="btnremove_{{itemId._id}}" class="btn btn-danger btn-remove-item" data-btnremoveitemid="{{item._id}}"> <span class="glyphicon glyphicon-remove" aria-hidden="true"></span></a>
                </div>
            </div>
          </div>
          {{description}}

        </div>

    {{/each}}

{{else}}
  <div class="message">No items</div>
{{/if}}
